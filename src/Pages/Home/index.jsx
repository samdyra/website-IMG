import React from "react";
import style from "./style.module.scss";

const HomeScreen = () => {
  const { container } = style;
  return <div className={container}>Website IMG</div>;
};

export default HomeScreen;
